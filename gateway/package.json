{
  "name": "api-gateway",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc"
  },
  "dependencies": {
    "fastify": "^4.x",
    "fastify-jwt": "^6.x",
    "fastify-rate-limit": "^7.x",
    "fastify-http-proxy": "^7.x",
    "dotenv": "^16.x"
  },
  "devDependencies": {
    "ts-node-dev": "^2.x",
    "typescript": "^5.x"
  }
}
